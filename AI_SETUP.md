# 🤖 Настройка AI провайдеров

Приложение поддерживает несколько AI провайдеров для генерации текста и голоса.

## 🔑 Настройка API ключей

### 1. Создайте файл `.env.local`:

```env
# OpenAI (для ChatGPT и TTS)
VITE_OPENAI_API_KEY=sk-your-openai-key-here

# Gemini (Google)
VITE_GEMINI_API_KEY=your-gemini-key-here
```

### 2. Получите API ключи:

#### OpenAI:

1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в https://platform.openai.com/api-keys
3. Создайте новый API ключ
4. Скопируйте и вставьте в `.env.local`

#### Gemini:

1. Перейдите на https://aistudio.google.com/app/apikey
2. Создайте API ключ
3. Скопируйте и вставьте в `.env.local`

---

## ⚙️ Выбор провайдера

Откройте файл `services/aiConfig.ts` и настройте:

```typescript
export const AI_CONFIG = {
  // Провайдер для текста
  textProvider: "openai", // 'gemini' | 'openai'

  // Провайдер для голоса
  voiceProvider: "openai", // 'gemini' | 'openai'

  // Настройки OpenAI
  openai: {
    textModel: "gpt-3.5-turbo", // или 'gpt-4'
    voiceModel: "tts-1", // или 'tts-1-hd'
    voice: "nova", // alloy, echo, fable, onyx, nova, shimmer
    speed: 0.9,
  },
};
```

---

## 💰 Стоимость

### OpenAI:

- **GPT-3.5-turbo**: ~$0.002/1K токенов (~$0.02 за 100 поздравлений)
- **GPT-4**: ~$0.03/1K токенов
- **TTS**: $15/1M символов (~$0.05 за 100 поздравлений)

### Gemini:

- **Бесплатно**: 15 запросов в минуту, 1500 в день (с ограничениями)
- **Платно**: от $0.125/1M токенов

---

## 🎤 Голоса OpenAI TTS

Доступные голоса:

- **alloy** - нейтральный
- **echo** - мужской
- **fable** - британский акцент
- **onyx** - глубокий мужской
- **nova** - женский (рекомендуется) ⭐
- **shimmer** - женский выразительный

Скорость речи: `0.25` - `4.0` (рекомендуется `0.9` для торжественности)

---

## 🔄 Как переключать провайдеров

### Вариант 1: Только OpenAI (рекомендуется)

```typescript
textProvider: 'openai',
voiceProvider: 'openai',
```

### Вариант 2: Только Gemini

```typescript
textProvider: 'gemini',
voiceProvider: 'gemini',
```

### Вариант 3: Гибрид (оптимально)

```typescript
textProvider: 'openai',  // Быстрее и дешевле
voiceProvider: 'openai', // Качественный TTS
```

---

## 🧪 Тестирование

После настройки:

1. Перезапустите dev server: `npm run dev`
2. Откройте консоль браузера (F12)
3. Создайте открытку
4. В консоли увидите:
   - `🤖 Используем openai для генерации текста`
   - `🎤 Используем openai для генерации голоса`

---

## ❗ Troubleshooting

### Ошибка "API key not found"

- Проверьте файл `.env.local`
- Перезапустите dev server
- Убедитесь, что ключи начинаются с `VITE_`

### Голос не воспроизводится

- Проверьте консоль браузера на ошибки
- Убедитесь в наличии API ключа
- Проверьте квоту на аккаунте OpenAI

### Текст генерируется, но голоса нет

- Установите `voiceProvider: 'openai'` в `aiConfig.ts`
- Проверьте баланс на аккаунте

---

**Рекомендация**: Используйте OpenAI для обоих провайдеров - это самое надежное и качественное решение! ✅
