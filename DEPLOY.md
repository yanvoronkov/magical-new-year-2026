# 🚀 Деплой на EasyPanel через Docker

Инструкция по развертыванию приложения на вашем сервере с помощью EasyPanel.

## 📋 Предварительные требования

1. **Сервер** с установленным Docker
2. **EasyPanel** установлен на сервере
3. **GitHub** репозиторий с кодом
4. **OpenAI API ключ** (получить на https://platform.openai.com/api-keys)

---

## 🔧 Шаг 1: Подготовка на GitHub

### Создайте `.env` файл в репозитории (или настройте в EasyPanel):

```env
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here
```

⚠️ **Важно**: Не коммитьте `.env` с реальными ключами! Используйте переменные окружения в EasyPanel.

---

## 📦 Шаг 2: Настройка в EasyPanel

### 1. Войдите в EasyPanel

Откройте панель управления EasyPanel на вашем сервере.

### 2. Создайте новый проект

- Нажмите **"Create Project"**
- Выберите **"Deploy from GitHub"**

### 3. Настройте репозиторий

- **Repository**: `https://github.com/yanvoronkov/magical-new-year-2026`
- **Branch**: `main`
- **Build Method**: **Docker**

### 4. Настройте переменные окружения

В разделе **Environment Variables** добавьте:

```
VITE_OPENAI_API_KEY=sk-your-actual-key-here
```

### 5. Настройте порт

- **Container Port**: `80`
- **Public Port**: `80` или любой другой

### 6. Настройте домен (опционально)

Привяжите ваш домен к проекту в разделе **Domains**.

---

## 🐳 Локальная сборка Docker (для тестирования)

### Сборка образа:

```bash
docker build -t magical-new-year-2026 .
```

### Запуск контейнера:

```bash
docker run -d \
  -p 8080:80 \
  --name new-year-app \
  magical-new-year-2026
```

Приложение будет доступно на `http://localhost:8080`

---

## 🔒 Безопасность

### ⚠️ Никогда не коммитьте:

- `.env.local` с реальными API ключами
- Файлы с секретами

### ✅ Правильный способ:

1. Храните ключи в **переменных окружения** EasyPanel
2. Используйте `.env.example` как шаблон
3. Добавьте `.env*` в `.gitignore`

---

## 📊 После деплоя

### Проверьте работу:

1. Откройте ваш домен или IP
2. Создайте тестовую открытку
3. Проверьте работу голоса
4. Проверьте консоль браузера на ошибки

### Логи в EasyPanel:

```
View Logs → Application Logs
```

---

## 🔄 Обновление приложения

После изменений в коде:

1. **Commit** и **Push** на GitHub
2. EasyPanel **автоматически** пересоберет и развернет новую версию

Или вручную:

```
Project → Redeploy
```

---

## ⚙️ Настройки Production

### Изменить голос:

Файл `services/aiConfig.ts`:

```typescript
voice: 'nova', // Выберите из доступных
```

### Изменить громкость:

Файл `App.tsx` (строка ~125):

```typescript
gainNode.gain.setValueAtTime(0.1, ctx.currentTime); // Музыка
voiceGain.gain.setValueAtTime(1.5, ctx.currentTime); // Голос
```

---

## 🐛 Troubleshooting

### Проблема: "API key not found"

- Убедитесь, что переменная `VITE_OPENAI_API_KEY` добавлена в EasyPanel
- Имя должно начинаться с `VITE_`
- Пересоберите проект после добавления переменных

### Проблема: "Build failed"

- Проверьте логи сборки в EasyPanel
- Убедитесь, что `package.json` и `package-lock.json` в репозитории

### Проблема: "Container не запускается"

- Проверьте, что порт 80 открыт
- Проверьте логи контейнера

---

## 📝 Полезные команды Docker

```bash
# Просмотр логов
docker logs new-year-app

# Остановка контейнера
docker stop new-year-app

# Удаление контейнера
docker rm new-year-app

# Удаление образа
docker rmi magical-new-year-2026
```

---

## 🎉 Готово!

Ваше приложение развернуто и доступно пользователям!

**URL**: https://ваш-домен.com (или IP сервера)

---

_Создано: 31 декабря 2025_
